public class Main {

	public static void main(String[] args) {
		Credentials credentials = new Credentials();
		new LoginPage(credentials.getCredentials());
		
		//AdminMenuPage adminMenu = new AdminMenuPage();
		
	}
}
